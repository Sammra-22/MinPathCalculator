name := "MinimalPathCalculator"

version := "0.1"

scalaVersion := "2.12.6"

libraryDependencies += "org.scalatest" %% "scalatest" % "3.0.5" % "test"

/*
scalaSource in (Compile, run) := baseDirectory.value / "src" / "main" / "scala"

mainClass in(Compile, run) := Some("com.utility.minimizer.Hello")
*/
